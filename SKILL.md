---
name: "stock-bottom-fishing"
description: "A股个股多维技术分析，评估是否适合抄底/潜伏，并管理持仓处置与逃顶止盈。整合TD神奇九转、MACD、KDJ、RSI、WR、CCI、BIAS、DMI/ADX、OBV、ATR、BOLL、均线、量能、背离、周线、主力资金流、筹码分布，并补充BBI/TRIX/ROC/VR/MFI/AR·BR/XSII 七个抄底辅助维度与 SAR 抛物线转向（指标原语移植自 MyTT/通达信口径）。抄底与逃顶共用同一套三层漏斗的镜像版本：底部看摆动低点+底背离+0轴下金叉，顶部看摆动高点+顶背离+0轴上死叉+SAR 移动止损线。当用户给出个股名称/代码并询问能否抄底、是否见底、是否适合低吸潜伏、底部信号分析，或询问 MACD 金叉会否反弹、持仓该怎么处理、是否该卖出/减仓/止盈、是否见顶时触发。"
---

# A股抄底/潜伏多维技术分析

## 定位

对单只 A 股做"自下而上"的全维度技术体检，输出"当前是否适合抄底/潜伏"的结构化结论；**同一套漏斗上下颠倒着用**，即可输出"当前是否该卖出/减仓/止盈"的逃顶结论；对多只持仓做横向体检与处置提示；对 MACD 金叉类问题做路径推演 + 历史复盘定价。适用于下跌趋势中的个股见底判断、超跌反弹机会识别、左侧潜伏评估、持仓风险体检与**高位逃顶/止盈**。**只做技术分析参考，不构成投资建议；对游资妖股、高估值题材股须额外提示风险。**

## 文件结构

```
SKILL.md                       本文件（总纲：维度、TD规则、判定框架、脚本矩阵）
README.md                      面向使用者的说明与安装
references/report-template.md  ★ 固定报告模版（5 套模版 + 字段来源对照表 + 实例）
references/playbook.md         ★ 思考逻辑手册（三层漏斗、十条原则、四档矩阵、**顶部镜像矩阵与止盈体系**、标签表、SOP）
references/troubleshooting.md  ★ 避坑清单（代理/K线参数/筹码算法/**数据层韧性机制**/报错速查）
references/mytt-merge-plan.md  MyTT 指标库合并方案与实施进度（P0–P6，含原语清单与取舍理由）
scripts/stock_lib.py           统一数据层：抓数 + 指标 + 筹码 + 价位（唯一的口径来源）
scripts/analyze_stock.py       单只 18 维体检 + 7 维补充 + 顶部信号块（文本报告）
scripts/holdings_check.py      持仓/多标的批量体检与处置提示
scripts/macd_path.py           MACD 金叉路径模拟（何时金叉、金叉价、所需涨幅）
scripts/macd_stats.py          历史复盘：0轴下金叉（抄底侧）/ 0轴上死叉·TD红9（顶部侧）
```

## 何时使用（触发条件）

- 用户输入**股票名称或 6 位代码**，要求判断：能否抄底 / 是否见底 / 适合低吸吗 / 底部到了吗 / 绿9出现能不能买 / 想潜伏这只票
- 用户要求用"神奇九转 / TD 序列 / MACD / KDJ"等指标综合分析某只个股
- 用户说"**MACD 金叉了，会不会反弹**""什么时候金叉"——须跑 `macd_path.py` + `macd_stats.py`，**不得只凭直觉作答**
- 用户问"**该不该卖 / 要不要减仓 / 到顶了吗 / 还能拿吗 / 止盈位设在哪**"——走逃顶侧流程（`analyze_stock.py` 的 `[顶部信号]` 块 + `macd_stats.py --side top` + `playbook.md` §五 顶部四档镜像矩阵）
- 用户报出**多只标的或持仓成本**，问"这几只怎么处理"——用 `holdings_check.py` 横向体检
- 注意：若用户询问多只股票横向对比，对每只分别执行本流程后横向汇总

## 工作流程

**先取数、后定性、再按模版成文。** 任何结论都不得脱离脚本输出的数据。

1. **解析标的**：名称或代码均可（`stock_lib.resolve_symbol` 走东财搜索接口自动解析，含 sh/sz 前缀与 ETF 识别）。同名多只时让用户给代码。
2. **按问题类型选脚本跑数**：

   | 用户问题 | 命令 |
   |---|---|
   | 单只能否抄底/见底/潜伏 | `python3 scripts/analyze_stock.py <代码或名称>` |
   | 该不该卖/到顶了吗/止盈位 | `python3 scripts/analyze_stock.py <代码>` + `python3 scripts/macd_stats.py <代码> --side top` |
   | 多只/持仓怎么处理 | `python3 scripts/holdings_check.py <代码[:成本[:股数]]>... [--detail]` |
   | MACD 金叉会否反弹 | `python3 scripts/macd_path.py <代码>` + `python3 scripts/macd_stats.py <代码>` |
   | 只要一句话结论 | 跑 `analyze_stock.py` 后按模版 D 作答 |

3. **按"三层漏斗"顺序解读**（位置 → 趋势 → 动能资金 → 价位），规则见 `references/playbook.md`；**逃顶侧把同一漏斗镜像使用**（涨到位没有 → SAR 是否翻空 → 超买与出货 → 在哪减），规则见 `playbook.md` §五。
4. **按"综合判定"给出四档结论之一**（抄底侧四档见下方；逃顶侧四档 = 顶部确认 / 顶部警示 / 持仓观察 / 趋势健康），写清触发清单、关键价位与失效条件。
5. **按 `references/report-template.md` 的固定模版成文**（模版 A 单只抄底 / B 持仓 / C MACD专题 / D 速答 / E 逃顶止盈），中文、表格化，末尾附免责声明。
6. **遇到抓数失败**，查 `references/troubleshooting.md`，并在报告中标注缺失维度，**不得臆造数据**。

## 分析维度全景与解读规则

| # | 维度 | 关键观察点 | 抄底语境下的解读 |
|---|---|---|---|
| 1 | TD 神奇九转 | 绿9/红9、当前计数、是否延长 | 见下方专节 |
| 2 | MACD(12,26,9) | DIF/DEA 与 0 轴关系、柱体方向 | 0 轴下绿柱**连续收窄**=动能衰竭(偏多)；绿柱仍扩大=恶化(偏空)；0 轴上红柱=强势 |
| 3 | KDJ(9,3,3) | K/D 金叉死叉、J 位置 | 低位(<30)金叉=偏多；低位死叉=仍在走弱；J<0 或 <20=超卖 |
| 4 | RSI(6/12/24) | 6 日超卖/超买 | 6 日 <20 极端超卖；<40 偏弱；底部区域回升=修复 |
| 5 | WR(14) | >80 超卖 <20 超买 | 超卖区支持反弹需求 |
| 6 | CCI(14) | <-100 超卖 >100 超买 | 从 <-100 回升=潜在转强 |
| 7 | BIAS(6/24) | 乖离程度 | 24 日乖离 < -15% 属深度超跌(历史上易反弹)；<-25% 极端 |
| 8 | DMI(14)/ADX | +DI 与 -DI、ADX 强度 | **-DI 明显>+DI 且 ADX≥25=强下跌趋势未止(重大减分)**；ADX 走低+DI 上穿=转多苗头 |
| 9 | OBV | 累积量能方向 | 价格下跌而 OBV 走平/上行=资金暗中吸筹(加分) |
| 10 | ATR(14) | 日均波幅 | 用于设定止损距离(一般 ≥1.5×ATR)，防止被正常波动扫损 |
| 11 | BOLL(20,2) | 价格相对轨位、带宽 | 触及/跌破下轨后收回=超跌；中轨下=弱，站上中轨=转强 |
| 12 | 均线系统 | MA5/10/20/60/120/250 排列 | 空头排列+价格远离 MA120/MA250=超跌；站回 MA5=短线转强第一信号；MA120/250 支撑有效性关键 |
| 13 | 量能 | 5 日均量/20 日均量、下跌与反弹量对比 | 缩量阴跌=抛压减轻但无承接(中性)；**放量阳线=资金进场(强确认)**；放量下跌=出货(减分) |
| 14 | 底背离检测 | 近 30 日双低点与 DIF/RSI | 价格新低而 DIF 抬高=底背离(强加分，常预示反转) |
| 15 | 周线 | 周 MACD/KDJ、周 MA5/10 | 日线超跌但周线绿柱扩大=反弹高度受限(矛盾，减分)；日周共振企稳=高胜率 |
| 16 | 主力资金流 | 近 5/10 日主力净流入 | 持续净流出(尤其反弹日出货)=减分；连续 2 日+净流入=吸筹(加分) |
| 17 | 筹码分布 | 获利比例、平均成本、90% 成本区 | 获利比例<10%=几乎无获利抛压(底部特征)；现价低于 90% 成本区下沿=套牢沉重(反弹到成本区有压力) |
| 18 | 位置 | 距 60/250 日高低位置 | 高位回撤 40%+ vs 低位整理，含义完全不同；先辨位置再看信号 |

## 补充维度（19~25，抄底辅助）

这七个维度是 P1 从 MyTT/通达信原语库按需移植时补入的，**只取对"判底"有信息增益的部分**。它们**不单独定结论**，只用于给前 18 维的结论加/减分与交叉验证；取舍理由与未纳入项（TAQ/KTN/EXPMA/MASS/ASI/DPO/EMV/CR/DFMA）见 `references/mytt-merge-plan.md`。

| # | 维度 | 字段 | 抄底语境下的解读 |
|---|---|---|---|
| 19 | BBI 多空指标 | `bbi` | = (MA3+MA6+MA12+MA24)/4。价格在 BBI 上方=中期偏多；**下穿 BBI 后首次收回**是较好的左侧转强信号，比单看 MA20 更钝、更少骗线 |
| 20 | TRIX(12) | `trix` / `trma` | 三重指数平滑，滤噪强、滞后大。**TRIX 上穿 TRMA（金叉）+ 位于 0 轴附近**=中期动能转折；深跌后 TRIX 由负转平再上翘，比 MACD 更不易被单日波动打断 |
| 21 | ROC(12) | `roc12` / `maroc` | 12 日变动率。ROC 上穿其均线=短期动能转强；**ROC 在 -10% 以下的极低位回升**是超跌反弹的常见前兆（注：原库的 MTM 因与 ROC 高度共线，未纳入） |
| 22 | VR(26) | `vr26` | 成交量比率 = 上涨日量能 / 下跌日量能。**VR < 40~70 属地量区**（下跌动能枯竭，偏多）；VR 突然放大且价升=资金进场确认；VR 高位(>350)则相反 |
| 23 | MFI(14) | `mfi14` | 带量能的 RSI。**MFI < 20 = 量价双重超卖**，比单纯 RSI 超卖更可靠；价格新低而 MFI 抬高=**量能维度底背离**（与第 14 维 DIF 背离互为印证） |
| 24 | AR(26)/BR(26) | `ar26` / `br26` | 人气/意愿指标。**AR < 60 且 BR < 50** 属人气冰点，常见于底部区域；AR 上穿 BR=人气回暖；两值极低时不宜再看空 |
| 25 | XSII(13,5) | `xsii_up/xsii_dn/xsii_up2/xsii_dn2` | "薛斯通道"式极值：`up2/dn2` 为极端外轨。**跌破 dn2 后收回**=极端超卖反抽；冲击 up2=极端超买。用于给"位置"维度提供通道口径的第二把尺 |

**叠加原则**：补充维度与主维度**同向时才加权**。例如「第 21 维 ROC 极低位回升 + 第 23 维 MFI 背离 + 第 2 维 MACD 绿柱收窄」三者同向 = 动能层面证据充分；若补充维度与主维度矛盾（如 MFI 超卖但主力仍在放量净流出），应**以主维度（资金/趋势）为准**，并把矛盾如实写入报告的"风险证据"。

## 神奇九转（TD Sequence）专节

**规则（标准 Setup）**：
- 买入计数（绿 9）：连续 K 线，每根收盘价 < 4 根前的收盘价，从第 1 根起计数，至 9 为"底部 9"（绿9）
- 卖出计数（红 9）：对称，收盘价 > 4 根前收盘价连续 9 根
- 中断：计数 1-8 阶段任意一根不满足条件即清零重计；达到 9 后若延续则进入"延长"(绿10/11…)

**正确用法（关键）**：
1. **绿9 是"连续下跌 9 段的统计标记"，是转折预警，不是买入指令**。出现绿9 只说明下跌动能可能耗尽，是否真见底必须看辅助指标与后续确认。
2. 最常见的可靠买法是"**绿9 + 次日/近期第一根放量阳线确认**"（TD 9 后买阳不买阴）。
3. 绿9 当根收长下影(锤子线)或绿9 出现在关键支撑(MA120/前期平台/前低)上方，可信度大增。
4. **极弱趋势中绿9 后常延长**（9 后继续绿10/11/12），所以绿9 单独出现≠可以买。
5. 若辅助指标（MACD 绿柱扩大、KDJ 死叉、主力净流出、周线恶化）全部不配合=“裸绿9”，应视为下跌中继而非底。

**判定指引**：
- 绿9 + MACD 绿柱收窄 + KDJ 低位金叉 → 底部结构较好，等放量阳线确认
- 绿9 + MACD 绿柱仍扩大 + KDJ 死叉 → 大概率只是下跌中继，禁左侧接刀
- 无绿9 但已出现"绿8"→ 观察是否构成 9，不提前行动
- 红9(顶部信号)出现 → 走顶部/持仓侧流程（见下方「顶部与逃顶（镜像能力）」专节）。**红9 是预警不是卖令**，可靠变盘 = 红9 + 次日/近期第一根放量阴线（卖阴不卖阳）；DIF≤0 时的红9 才是真压制，涨势中(DIF>0)的红9 常是"中继"

## 综合判定框架

将各维度按以下权重分层（抄底语境）：
- **强负面(一票否决级)**：周线 MACD 绿柱扩大且 DIF 下穿 0 轴、放量破位创新低、主力连续大幅净流出、妖股退潮期
- **强正面**：日线 MACD 绿柱 5 日+连续收窄、KDJ 低位金叉、TD 绿9 出现在关键支撑上、MACD 底背离、放量阳线站回 MA5
- **中性观察**：缩量、BIAS 温和负乖离、WR/CCI 超卖

结论输出为四档之一：
1. **强势买点（右侧确认）**：绿9(或超跌形态) + 放量阳线站回 MA5 + 主力转净流入 → 给出介入价/止损/分批计划
2. **左侧潜伏区（可小仓试探）**：底部结构好但缺右侧确认 → 给出"出现 XXX 信号即加仓"的触发清单
3. **等待区（暂不参与）**：部分信号偏多但关键确认缺失（如资金仍流出/周线未稳）→ 给出等待条件
4. **不适合（回避）**：强负面信号主导（趋势未止/资金出逃/妖股退潮）→ 明确不建议并说明理由

**每份结论必须包含**：当前状态一句话、支持/反对证据各 2-3 条、关键价位(支撑/压力/止损)、触发清单(出现什么信号才行动)、免责声明。止损参考 = 关键支撑下方 1×ATR 左右。

## 顶部与逃顶（镜像能力）

**逃顶不是另建一套框架，而是把上面三层漏斗上下颠倒着用**，唯一的差别是"信号方向反转、读法词跟着反转"。完整规则见 `playbook.md` §五。

**SAR 抛物线转向（本技能唯一的"移动止损线"维度）**：取通达信口径（`stock_lib.t_sar`，源自 MyTT_plus 的 `TDX_SAR`）。语义三态：

| SAR 位置 | 含义 | 用法 |
|---|---|---|
| 在价格**下方** | 多头持有期 | SAR 即**移动止盈线**，随行情自动抬升，收盘不破就拿着 |
| 在价格**上方** | 空头/离场期 | SAR 是**压力线**，站上它才谈重新做多 |
| 当根上穿价格（`sar_flip`） | 由多转空 | 教科书式**离场信号** |

**顶部四档镜像矩阵**（支持与反对势均力敌 → 一律「持仓观察」）：

| 档位 | 必须满足的硬条件 |
|---|---|
| **顶部确认（理应离场）** | 收盘跌破 SAR **或** 跌破「最近支撑−1×ATR」；且主力转净流出 **或** 0 轴上死叉与顶背离同现 |
| **顶部警示（减仓保护）** | 超买/顶部标签 ≥3 项，但未跌破 SAR、趋势未坏 → 给分批减仓计划保住浮盈 |
| **持仓观察（持有但不加）** | 仅红柱收窄 / 距 250 日最高已回撤 >20%，未出现 0 轴上死叉且未破 SAR |
| **趋势健康（继续持有）** | 无标签或 ≤2 项 + SAR 多头持有期 + 红柱扩大 + 主力不逆 |

**必须纠正的两个直觉**：① **超买 ≠ 要跌**——强势股 J 值可在 80 上方钝化数周，标签只用于"减仓保护"、不用于"清仓判顶"；② **红9 ≠ 顶**——须放量阴线确认（卖阴不卖阳，与抄底侧"买阳不买阴"镜像）。

**顶部背离与历史定价**：顶背离用 `swing_highs()` + `divergence_top()`（与抄底侧的摆动低点口径严格镜像，**"仅 1 个摆动高点，结构不充分"是合法结论**，且**只在高位区（距 60 日低 >50%）才有意义**）；`macd_stats.py --side top` 分「0 轴上死叉」「TD 红9」两套口径给跌率与再金叉率——**跌率 ≈50% 或再金叉率 >60% 即判噪音**，只当提示、不单独作为离场依据。

**止盈体系（三档工具，复用 `levels()` 压力位）**：① SAR 移动止盈线（首选，把"何时卖"从判断题变成执行题）；② `levels()['resistances']` 压力位分 2~3 批减仓；③ 若 SAR 距现价 >15%（给的空间过大），改用「最近支撑 − 1×ATR」兜底。**三个触发器任一触发即执行**：收盘跌破 SAR；0 轴上死叉且顶背离成立；高位 + 超买标签 ≥3 项 + 当日主力大额净流出。**纪律：止盈计划必须在买入时就写下——报告给了买入建议却没给止盈触发器，视为不合格输出。**

## 思考逻辑（核心要点）

完整版见 `references/playbook.md`。以下是必须遵守的七条：

1. **三层漏斗定序**：先判**位置**（距 60 日低/高、MA120·250 上下）→ 再判**趋势**（DMI/均线/周线）→ 再判**动能与资金**（MACD柱/KDJ/量能/主力）→ 最后落到**价位**（支撑/压力/止损）。顺序错了结论必错：同一根绿9，低位是机会、半山腰是陷阱。
2. **信号是统计事件，不是买入指令**。回答"金叉会不会涨"必须用**该股自身历史同类信号**的中位收益/胜率/再死叉率来定价（`macd_stats.py`），而不是套用"金叉=涨"的直觉。中位≈0 或再死叉率>60% 即判为噪音。
3. **必须量化等待成本**。不说"等金叉"，要说"明日金叉需涨 20.3%（对照 ATR 不可能）；横盘则第 11 日、+1%/日则第 5 日"（`macd_path.py`）。
4. **量能可以放大也可以证伪信号**。极度缩量（5日/20日量比<60%）下的上涨不构成任何确认；放量阳线站回 MA5 才是右侧确认。
5. **背离即风险**：当日主力净流出而 5 日净流入，说明"护盘的是过去的钱，今天的钱在跑"，必须单独标注，不得取平均掩盖。
6. **纪律高于预测**：每个结论都要落一个具体的止损数字；数据缺失时如实标注并说明对可靠性的影响，绝不用其他维度"补"缺失维度。
7. **顶部侧读法要反转，直觉要反过来**：超买标签只用于"减仓保护"、不用于"清仓判顶"；红9 须放量阴线确认；**SAR 是事前约定的移动止盈线，收盘跌破即执行，不临时商量**。两侧的阈值与判定都收敛在 `stock_lib`（`divergence` / `divergence_top` / `top_signals` / `t_sar`），**上层脚本不得各写一套超买线或背离比较**。

## 脚本

**依赖**：仅 `requests pandas numpy`，**不需要 akshare**（本环境 akshare 会因系统代理报 ProxyError）。
```bash
pip install requests pandas numpy --break-system-packages
```
所有网络请求与指标口径**统一收敛在 `scripts/stock_lib.py`**（导入时自动清除系统代理并 `trust_env=False`）。改口径只改它，其余脚本只做渲染与判定。

| 脚本 | 作用 | 典型用法 |
|---|---|---|
| `stock_lib.py` | 统一数据层：解析标的 / 多源 K 线 / 实时快照 / 资金流 / 筹码 / 18维指标 / 关键价位 / **35 个通达信原语 + 顶部信号** | `python3 stock_lib.py 002552`（自检） |
| `analyze_stock.py` | 单只 18 维体检报告（末尾附 19~25 补充维度区块 + `[顶部信号]` 块：超买标签 / SAR 移动止盈线 / 回撤 / 0 轴上死叉 / 顶背离与摆动高点） | `python3 analyze_stock.py 002552`；`--no-chip` `--no-flow` |
| `holdings_check.py` | 多标的/持仓批量体检 + 风险标签 + 处置提示（**复用 `analyze_stock.render()`，自动含顶部信号块**） | `python3 holdings_check.py 002552:51.2 603330:9.8`；`--detail` `--no-market` `--file holdings.txt` |
| `macd_path.py` | 金叉路径模拟：何时金叉、金叉价、明日所需涨幅 | `python3 macd_path.py 002580 --days 15` |
| `macd_stats.py` | 历史复盘。抄底侧：0 轴下金叉的胜率/中位收益/再死叉率；**顶部侧（`--side top`）：0 轴上死叉与 TD 红9 的跌率/再金叉率** | `python3 macd_stats.py 002580`；`python3 macd_stats.py 002552 --side top`（别名 `--top` / `--sell`） |

**数据源与降级**：东财（首选，盘中含当日，带换手率）→ 腾讯（兜底，**无换手率 → 筹码不可算**）→ **本地缓存**（离线/限流时兜底，会标注新鲜度）。脚本会在 `[日线]` 行标注实际数据源；需要时在报告中如实标注降级情况。注意东财自身有三台主机（`push2his` / `push2` / `push2delay`），会**一台一台地**在连接层被拒（`RemoteDisconnected`），其中 `push2delay` 对历史类接口只会回空/残响应，救不了日K与逐日资金流。

**通用注意**：
- 东财 K 线**必须** `beg=0&end=20500101&lmt=<1600/800/500>`，写具体日期会 `RemoteDisconnected`（详见 `references/troubleshooting.md`）
- **筹码无服务端接口**，由 `stock_lib` 复刻东财前端 JS 算法（最近 210 根**不复权**日K + 换手率递归衰减 + 三角分布），获利比例是 0~1 小数，展示时乘 100
- 遇到限流/主机故障**不需要也不应该手动重跑**：数据层已自带冷却、预算、主机熔断与缓存降级（见下节），脚本会自适应并在报告中降级标注。只有**所有**降级路径都拿不到数据时才会报"日K所有数据源均失败"，此时才按 `references/troubleshooting.md` 排查网络
- ETF 无资金流与筹码，用 `--no-flow` / `--no-chip`；北交所(4/8 开头)与新股(上市 <60 日)指标参考意义有限，谨慎给结论
- 若某维度获取失败（网络），在报告中标注"该维度缺失"并说明对结论可靠性的影响，**不要臆造数据**

## 数据层韧性机制（stock_lib 内置）

本技能曾出现「连跑四脚本偶发 `日K所有数据源均失败`、单只看盘要等 80 秒」的问题，根因是**把"限流"当成了"重跑就好"的偶发事件**。现在数据层有七道闸门，全部在 `stock_lib.py`，脚本无需感知：

| 机制 | 常量 / 文件 | 作用 |
|---|---|---|
| **响应校验器** | `_rows_ok(n)`，经 `_get(ok=...)` 传入 | 拒绝"HTTP 200 但数据为空/只有 1 行"的**假成功**（`push2delay` 对历史接口的典型表现）。不通过则继续轮转下一台主机 |
| **限流冷却（指数升级）** | `COOLDOWN_SEC=20 → 40 → 80 → 120s`（`CD_MAX`），`_cooldown` + `_cdcnt` | 整源被判"病了"后，冷却期内上层整体走缓存降级；连续失败逐级加长，成功即清零 |
| **冷却闲置归零** | `CD_IDLE_RESET=180s` | 距上次失败超过 3 分钟，说明限流窗口已过，冷却计数自动归零，避免一次偶发把惩罚永久累加 |
| **等待预算** | `budget=<秒>` | 单次调用的总等待上限。**预算不够熬过冷却时直接返回 None**（而不是把剩余预算睡在冷却里、醒来仍失败）——这是当初 18 秒纯浪费的元凶 |
| **冷却期策略** | `wait_cd=True/False` | 必须硬试一次时传 `False`（只发一次、不等冷却）；`kline`/`fund_flow` 在冷却期内主动**短路到缓存**，不打接口 |
| **主机级熔断** | `HOST_DOWN_SEC=30s`，`_hostdown.json` | 某台主机在**连接层**被拒后，30 秒内轮转到它时直接跳过（不发请求、不占限速额度）。因为死掉的主机下一分钟仍然是死的，反复撞它只是白烧秒数 |
| **分档限速** | `MIN_INTERVAL=3s`（历史类）/ `LIGHT_INTERVAL=1s`（快照/指数/代码检索） | K 线接口有配额约束按 3s 走；行情快照类不背这个约束，按 1s 走，否则每份报告白等十几秒 |

**实测效果**（同一网络时段、同一命令）：

| 场景 | 改造前 | 改造后 |
|---|---|---|
| 单只全维分析（冷启动） | ~80s | **8.0s** |
| 四脚本连跑（冷缓存） | 80s+ 且偶发 `日K所有数据源均失败` | **12.4s，四个脚本全部 rc=0** |
| 四脚本连跑（热缓存） | — | **9.3s** |

**并发约束**：限速与冷却通过 `_lastreq` / `_cooldown` / `_hostdown.json` 三个落盘文件实现**跨进程**生效，因此"连跑多个脚本"是安全的；但**仍不建议并行（多进程同时）跑**——限速闸门只能减少并发冲突，不能替代串行。

## 输出模板

**固定模版见 `references/report-template.md`**，按问题类型五选一：

- **模版 A** 单只「抄底/潜伏评估」——最常用，含核心数据表、支持/反对证据、关键价位表、结论与触发清单
- **模版 B** 持仓「多标的横向体检」——横向表 + 逐只处置
- **模版 C** 「MACD 金叉专题」——先验前提、路径推演、历史复盘三块
- **模版 D** 一句话速答
- **模版 E** 单只「逃顶/止盈评估」——**买入建议与止盈触发器必须成对出现**

所有模版共同的三段式骨架：

```
### <名称>(<代码>) <评估类型> [数据截至 日期/盘中]
**一句话状态**：<趋势阶段 + 位置 + 主导信号>
| 维度 | 数值 | 信号 | ...（核心数据表）
**支持证据**：1. 2. 3.（每条带数值）
**反对/风险证据**：1. 2. 3.（每条带数值）
**关键价位**：支撑（由近及远）/ 压力 / 参考止损（= 最近支撑 − 1×ATR）
**结论**：<四档之一> + 触发清单 + 失效条件
**提示**：不构成投资建议
```

## 免责与边界

- 本技能输出为技术分析参考，不构成任何投资建议；最终决策由用户自负
- 对以下类型标的**默认提示高风险**：连续涨停/跌停的妖股、市盈率显著高于行业、公司已发风险警示公告、游资炒作特征明显（名称谐音概念炒作等）
- 大盘系统性风险（指数破位/政策冲击）会压制个股信号有效性，结论中宜注明"需结合大盘环境"
- 指标信号存在滞后与失效可能（尤其极端行情中的指标钝化），务必以"信号组合+确认"而非单一指标决策
